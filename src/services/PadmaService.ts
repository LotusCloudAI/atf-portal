export async function sendPadmaQuery(query: string): Promise<string> {
  // Placeholder — later connect to Gemini, Vertex, or Cloud Run
  return Promise.resolve("PADMA response (AI backend coming soon)");
}
